{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tags",
  "description": "Adds tag metadata to an existing schema",
  "type": "object",
  "properties": {
    "tags": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "tag" }
    }
  }
}
